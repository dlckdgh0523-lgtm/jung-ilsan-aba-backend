# Normalize line endings to LF in the repo — critical so Dockerfile, shell scripts,
# and the husky hook run correctly on the Linux deploy server.
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
